{
  "_comment": "This is documentation, not a real record. Every file in this folder (except this one) must follow this exact shape. Fields left as null mean 'this condition does not apply / is not used to screen this opportunity' and are skipped by the rules engine.",
  "opportunity_id": "string, unique, snake_case",
  "name": "string, official program name",
  "name_ur": "string, Urdu name",
  "category": "one of: scholarship | job | skills | assistance",
  "provider": "string, official issuing body",
  "province_scope": "one of: Balochistan | Punjab | Sindh | KP | AJK | GB | ICT | Federal | Nationwide",
  "target_group": "short string, who this is generally for",
  "summary_en": "1-2 sentence plain-language summary in English",
  "summary_ur": "1-2 sentence plain-language summary in Urdu",
  "eligibility_conditions": {
    "min_age": "integer or null",
    "max_age": "integer or null",
    "domicile_provinces": "list of province strings or null (null = no domicile restriction)",
    "min_education_level": "one of: matric | intermediate | bachelor | master | null",
    "min_marks_percentage": "number or null",
    "max_monthly_household_income": "number in PKR or null",
    "must_be_currently_enrolled": "true | false | null",
    "must_not_have_existing_scholarship": "true | false | null",
    "employment_status_required": "one of: unemployed | employed | any | null",
    "special_quota_note": "string or null, e.g. exemptions for orphans / BPS 1-4 govt employees' children",
    "min_experience_years": "number or null, used for job/category=job entries only",
    "application_deadline": "YYYY-MM-DD or null, used for job/category=job entries only (jobs go stale - always check this)"
  },
  "required_documents": ["list of strings"],
  "application_steps": ["list of strings, in order"],
  "official_url": "string, must be a real official URL",
  "source_title": "string, name of the page/document this was sourced from",
  "source_date": "YYYY-MM-DD, date the source page/document itself was published or last updated, if known",
  "last_verified": "YYYY-MM-DD, date a human on the team actually checked this record against the official source",
  "source_type": "curated | user_uploaded",
  "confidence_status": "verified | needs_recheck",
  "disclaimer": "string shown with this specific entry, if it needs one beyond the global banner"
}
